### Hi, I'm Ben 👋

Independent full-stack developer (7+ years) building production products end-to-end — architecture, backend, frontend, payments, deployment. Based in Bangkok 🇹🇭, working remote.

**Stack:** Next.js · React · Python · Rust · Node.js
**Currently focused on:** LLM/AI integration (Claude/Anthropic API), production pipelines, payment systems

---

#### 🚀 Things I've shipped

**[Voicica.ai](https://voicica.ai)** —Full-stack AI voice & creative platform, live in production with several thousand active users across web and native Android app (Google Play).
- Built solo end-to-end: Next.js 15 + TypeScript frontend, deployed on Cloudflare Workers edge network (zero cold starts, global <100ms TTFB)
- Integrated 5 AI providers (Azure TTS, Google TTS, Fish Audio, Gemini, Kie.ai) behind a unified API with async job queue (Cloudflare Queues) to handle long-running synthesis tasks
- Cross-platform from a single codebase — Web, PWA, and native iOS/Android via Capacitor with OTA updates bypassing app store review cycles
- Self-migrated production database from Neon PostgreSQL → Cloudflare D1 (28 tables, 60+ async call sites rewritten)
- Shipped full monetization stack: Stripe subscriptions + Google Play Billing IAP + token mining economy with USDT withdrawal (Polygon/BEP20/Solana)
- Supports 12 languages, 3-tier referral program, FCM push notifications, and a full admin dashboard

**[ReelWord](https://reelword.app)** — English-learning platform using TV/film clips with bilingual subtitles and spaced-repetition study tools. Built end-to-end: Next.js + Python + Cloudflare R2 + Neon Postgres, with a full freemium subscription/billing system.

**Multilingual content automation pipeline** — LLM-driven translation pipeline for short-form video, with timestamps inherited programmatically rather than generated by the model (deliberately keeping timing logic out of the LLM for reliability).

**Pyreth** —Production MEV bot on Base achieving same-block liquidation across Morpho Blue, Aave V3, Moonwell, and Compound V3.
- Core edge: forked op-node with a custom OracleHook (Go) that intercepts Chainlink transmit() calls and pushes price updates via Unix socket — measured 74 ms from oracle update to transaction submission, landing in the same Flashblock
- Built the full position-tracking pipeline: The Graph subgraph → incremental tx scan → Multicall3 batch health factor computation → Redis ZSET indices for sub-100 µs liquidation price lookups
- Forked Reth (Rust) for IPC socket access and patched a SenderRecovery prune bug; runs a co-located node stack (Reth + op-node) to eliminate external RPC latency entirely
- Integrated Chainlink SVR auction system (Atlas/SVR) with EIP-712-signed solver operations and multi-collateral HF impact estimation for bid scoring
- Multi-EOA concurrent submission with pre-synced nonces and keep-alive connections to Base sequencer; post-liquidation handler atomically cleans Redis + logs result + fires Telegram alert
- Full-stack observability: Next.js 16 ops dashboard with real-time node health, sync status, and liquidation analytics (protocol breakdown, top liquidators, P&L distribution)

---

#### 📫 Reach me

- Email: bensting19@gmail.com

*Open to remote full-stack / AI-integration roles and contract work.*
